# ╔══════════════════════════════════════════════════════════╗
# ║                   TRADEBOT • .gitignore                  ║
# ║            okunaklı • düzenli • güvenli yapı             ║
# ╚══════════════════════════════════════════════════════════╝

# ── 1) Python derleme/artık dosyaları ─────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg-info/
.eggs/
build/
dist/

# ── 2) Sanal ortamlar (virtualenv/conda/poetry) ──────────────────────────────
venv/
.venv/
env/
trade_env/
.trade_env/

# ── 3) Log dosyaları ──────────────────────────────────────────────────────────
*.log
log/
logs/
*/logs/
**/global_testler/
**/pw-browsers/
pw-browsers/


# ── 4) Veritabanları (SQLite) ────────────────────────────────────────────────
*.db
*.sqlite
*.sqlite3
*.db-journal
*.db-shm
*.db-wal

# ── 5) Genel gizli dosyalar (.env vb.) ───────────────────────────────────────
# Not: Örnek dosyalar takipte kalsın
.env
.env.*
!.env.example

# ── 6) Yapılandırmalar • globalislemler/config altında ───────────────────────
#  - Gerçek dosyaları (json/.env) IGNORE ET
#  - Sadece *.example* dosyalarını TAKİP ET
globalislemler/config/.env
globalislemler/config/*.json
!globalislemler/config/.env.example
!globalislemler/config/*.example
!globalislemler/config/*.example.json

# Kök dizinde yanlışlıkla kalabilecek gerçek config’ler:
config.json
symbols.json
telegram_config.json
secrets*.json

# ── 7) Cache / geçici dosyalar ───────────────────────────────────────────────
.cache/
*.cache
*.tmp
tmp/

# ── 8) IDE / OS dosyaları ────────────────────────────────────────────────────
.vscode/
*.code-workspace
.idea/
.DS_Store
Thumbs.db

# ── 9) Selenium / WebDriver artıkları ────────────────────────────────────────
chromedriver
chromedriver.exe
geckodriver
chromedriver.log
selenium-screenshots/

# ── 10) (İsteğe bağlı) büyük ham veriler ─────────────────────────────────────
# data/
# !data/README.md


# Local backups
*.bak
# Personal notes
yapilacaklar.txt
globalislemler/yapilacaklar.txt
*.bak.*

# -- 11) test ve analizler için kullanıland dosyalrın çıktılarını içeren dosyalar

veritabani/analiz_raporu.csv
veritabani/global_data_dna_raporu.json
veritabani/global_data.dbsqlite3
diffler/